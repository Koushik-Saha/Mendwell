# mendwell-connector

The WordPress plugin that is the only path Mendwell uses to write to a site (PROJECT_SPEC.md §8, SECURITY.md §2).

Not scaffolded yet. It lands with its own task: PHP 7.4+/WP 6.2+, zero third-party PHP dependencies, PHPUnit via `wp-env`.
