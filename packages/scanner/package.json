{
  "name": "@mendwell/scanner",
  "version": "0.0.0",
  "private": true,
  "description": "Crawler, checks (axe, meta, links, ssl, uptime), Lighthouse runner and the SSRF-safe fetcher.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@mendwell/core": "workspace:*",
    "playwright": "1.63.0"
  },
  "devDependencies": {
    "@mendwell/fixtures": "workspace:*"
  }
}
